# Governance domain

Canonical source ownership:

- `user.Shareholder`
- `user.Announcement`
- `user.Policy`
- `user.Meeting`
- `user.BoardResolution`

These models represent corporate governance, board/investor state,
organization-wide communication and policy/meeting/resolution records.

Historical Django labels, tables, migrations, URLs, permission keys and legacy
Python module paths remain compatible.
