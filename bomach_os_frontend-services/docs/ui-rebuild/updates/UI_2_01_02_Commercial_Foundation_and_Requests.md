# UI-2.01 and UI-2.02 — Commercial Foundation and Service Requests

The HTML prototype is the visual and interaction authority.

Implemented:

- exact `Commercial Flow` navigation group;
- `Service Requests` badge `1`;
- `Quotations`;
- `Invoices & Payments`;
- `Approvals` alert badge `4`;
- typed commercial workspace;
- TanStack Query keys/options/API;
- MSW handler and realistic prototype seed data;
- literal Service Request KPI strip;
- search, status, branch and service filters;
- pixel-oriented request register with internal responsive scrolling;
- loading, error, retry and empty states.

The New Request workspace is UI-2.03. Request 360 is UI-2.04.
